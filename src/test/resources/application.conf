db = {
  driver = org.postgresql.Driver
  url = "jdbc:postgresql://test-db/postgres"
  username = postgres
  password = postgres
  num-threads = 15
  max-pool-size = 5
}

jwt = {
  secret = "secret"
}