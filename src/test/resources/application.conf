db = {
  driver = org.postgresql.Driver
  url = "jdbc:postgresql://localhost:5434/postgres"
  username = postgres
  password = postgres
  num-threads = 5
  max-pool-size = 2
}

jwt = {
  secret = "secret"
}